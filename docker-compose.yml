version: "3.8"

services:
  monitor:
    build: .
    ports:
      - "5000:5000"
    volumes:
      - ./monitor.db:/app/monitor.db
      - ./config.yaml:/app/config.yaml:ro     # 可覆盖默认配置
    restart: unless-stopped